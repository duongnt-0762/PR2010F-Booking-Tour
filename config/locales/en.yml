en:
  admin:
    requests:
      show:
        accepted:"Accepted"
        rejected:"Rejected"
        not_set:"Not yet confirmed"
          booking_now!:"Booking now"
  users:
  show:
    account: 'Account'
  requests:
    new:
      value: "$%{price}"
